SSH_PUBLIC_KEY="<%= user_data.ssh_keys.reject { |elm| elm.include?('"') }.sort.join("\n") -%>"
SSH_KEY="<%= user_data.ssh_keys.reject { |elm| elm.include?('"') }.sort.join("\n") -%>"
EMAIL="<%= user_data.mail -%>"
NAME="<%= user_data.full_name -%>"
X509_DN="<%= user_data.cert_dns.sort.join('|') -%>"
KRB_PRINCIPAL="<%= user_data.krb_principals.sort.join('|') -%>"
LAST_UPDATE="<%= DateTime.now.to_s -%>"
TOKEN_PASSWORD="<%= old_token_password -%>"
SUNSTONE=[
 DEFAULT_VIEW="<%= old_default_view -%>",
 LANG="<%= old_lang -%>",
 TABLE_ORDER="<%= old_table_order -%>"
]
VNC_WSS="<%= old_vnc_wss -%>"
