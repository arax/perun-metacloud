SSH_PUBLIC_KEY="<%= user_data.ssh_keys.join("\n") -%>"
EMAIL="<%= user_data.mail -%>"
NAME="<%= user_data.full_name -%>"
X509_DN="<%= user_data.cert_dns.join('|') -%>"
KRB_PRINCIPAL="<%= user_data.krb_principals.join('|') -%>"
LAST_UPDATE="<%= DateTime.now.to_s -%>"
TOKEN_PASSWORD="<%= old_token_password -%>"
